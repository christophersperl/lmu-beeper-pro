<Window x:Class="LMUWeaver.BrakeOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Brake Overlay" Width="180" Height="72" MinWidth="120" MinHeight="52"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" MouseDown="Window_MouseDown">

    <Window.Resources>
        <DrawingBrush x:Key="CarbonBrush" TileMode="Tile" Viewport="0,0,8,8" ViewportUnits="Absolute">
            <DrawingBrush.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Brush="#080808">
                        <GeometryDrawing.Geometry><RectangleGeometry Rect="0,0,8,8"/></GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing Brush="#151515">
                        <GeometryDrawing.Geometry>
                            <GeometryGroup>
                                <RectangleGeometry Rect="0,0,4,4"/>
                                <RectangleGeometry Rect="4,4,4,4"/>
                            </GeometryGroup>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingBrush.Drawing>
        </DrawingBrush>
    </Window.Resources>

    <Grid>
        <Border x:Name="BgLayer" Background="{StaticResource CarbonBrush}" BorderBrush="#33FFFFFF" BorderThickness="1"/>

        <Border Padding="8,5,8,6">
            <StackPanel>
                <!-- Label row -->
                <TextBlock Text="BRAKE IN" Foreground="#33FFFFFF" FontSize="7" FontWeight="Bold"
                           HorizontalAlignment="Center" Margin="0,0,0,1"/>

                <!-- Big distance number -->
                <TextBlock x:Name="TxtDist" Text="--- m" FontSize="24" FontWeight="Bold"
                           HorizontalAlignment="Center" Foreground="#FF00FFCC" Margin="0,-2,0,3"/>

                <!-- Proximity bar container -->
                <Grid x:Name="ProxContainer" Height="5">
                    <Border Background="#12FFFFFF" CornerRadius="2"/>
                    <Border x:Name="ProxBar" Background="#FF00FFCC" CornerRadius="2"
                            HorizontalAlignment="Left" Width="0"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Close button -->
        <Button Content="✕" Click="BtnClose_Click" HorizontalAlignment="Right" VerticalAlignment="Top"
                Width="14" Height="14" Background="Transparent" Foreground="#44FFFFFF" FontSize="9"
                Margin="0,2,2,0" Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="#CCFFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <!-- Resize grip -->
        <Thumb Width="8" Height="8" HorizontalAlignment="Right" VerticalAlignment="Bottom"
               Cursor="SizeNWSE" DragDelta="ResizeThumb_DragDelta" Margin="0,0,1,1">
            <Thumb.Template>
                <ControlTemplate TargetType="Thumb">
                    <Path Data="M 0,8 L 8,0 L 8,8 Z" Fill="#22FFFFFF"/>
                </ControlTemplate>
            </Thumb.Template>
        </Thumb>
    </Grid>
</Window>
