<Window x:Class="LMUWeaver.BrakeOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Brake Overlay" Width="260" Height="20" MinWidth="80" MinHeight="10"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" MouseDown="Window_MouseDown" MouseWheel="Window_MouseWheel"
        MouseRightButtonDown="Window_RightClick">

    <Window.Resources>
        <DrawingBrush x:Key="CarbonBrush" TileMode="Tile" Viewport="0,0,8,8" ViewportUnits="Absolute">
            <DrawingBrush.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Brush="#080808">
                        <GeometryDrawing.Geometry><RectangleGeometry Rect="0,0,8,8"/></GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing Brush="#151515">
                        <GeometryDrawing.Geometry>
                            <GeometryGroup>
                                <RectangleGeometry Rect="0,0,4,4"/>
                                <RectangleGeometry Rect="4,4,4,4"/>
                            </GeometryGroup>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingBrush.Drawing>
        </DrawingBrush>
    </Window.Resources>

    <Grid>
        <!-- Background layer — opacity via mouse wheel -->
        <Border x:Name="BgLayer" Background="{StaticResource CarbonBrush}" BorderBrush="#22FFFFFF" BorderThickness="1" Opacity="0.85"/>

        <!-- Bar: track (dark) + fill (colored) + leading edge highlight -->
        <Grid x:Name="ProxContainer" Margin="1">
            <Border Background="#0EFFFFFF"/>
            <Border x:Name="ProxBar" HorizontalAlignment="Left" Width="0">
                <Border.Background>
                    <SolidColorBrush x:Name="ProxBrush" Color="#FF00FFCC"/>
                </Border.Background>
            </Border>
            <!-- Bright leading-edge line -->
            <Border x:Name="ProxEdge" HorizontalAlignment="Left" Width="2" Background="#AAFFFFFF" Margin="0"/>
        </Grid>

        <!-- Resize grip (barely visible, bottom-right) -->
        <Thumb Width="6" Height="6" HorizontalAlignment="Right" VerticalAlignment="Bottom"
               Cursor="SizeNWSE" DragDelta="ResizeThumb_DragDelta">
            <Thumb.Template>
                <ControlTemplate TargetType="Thumb">
                    <Path Data="M 0,6 L 6,0 L 6,6 Z" Fill="#22FFFFFF"/>
                </ControlTemplate>
            </Thumb.Template>
        </Thumb>
    </Grid>
</Window>
